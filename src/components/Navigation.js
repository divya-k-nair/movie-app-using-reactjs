import { AppBar, Toolbar } from '@material-ui/core'
import React from 'react'

export default function Navigation() {
    return (
        <div>
            <AppBar>
                <Toolbar>MOVIE</Toolbar>
            </AppBar>
        </div>
    )
}
